import { INITIAL_PRODUCTS, INITIAL_ORDERS, INITIAL_SUPPLIERS, INITIAL_EXPENSES, INITIAL_SETTINGS } from './data/mockData.js'; import { createClient } from '@supabase/supabase-js'; const supabase = createClient('https://qnbwyblbxtwubmuejwtp.supabase.co', 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InFuYnd5YmxieHR3dWJtdWVqd3RwIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MzEwMTAwNSwiZXhwIjoyMDk4Njc3MDA1fQ.BpkEuIRVptszobrCUUjzUu4kItinpWU8lVQzdbL-pq8'); async function migrate() { console.log('Migrating products...'); for (const p of INITIAL_PRODUCTS) { const { error } = await supabase.from('products').insert({ title: p.title, category: p.category, purchasePrice: p.purchasePrice, price: p.price, oldPrice: p.oldPrice || null, supplier: p.supplier, images: p.images || [], barcode: p.barcode, description: p.description, colorVariants: p.colorVariants }); if (error) console.error(error); } console.log('Migrating suppliers...'); for (const s of INITIAL_SUPPLIERS) { const { error } = await supabase.from('suppliers').insert({ name: s.name, phone: s.phone, balance: 0 }); if (error) console.error(error); } console.log('Migrating expenses...'); for (const e of INITIAL_EXPENSES) { const { error } = await supabase.from('expenses').insert({ description: e.title, amount: e.amount, date: new Date().toISOString() }); if (error) console.error(error); } console.log('Migrating settings...'); for (const key in INITIAL_SETTINGS) { const { error } = await supabase.from('settings').insert({ key: key, value: INITIAL_SETTINGS[key] }); if (error) console.error(error); } console.log('Done.'); } migrate();
